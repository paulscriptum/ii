# Подключение к OpenClaw через браузер

## ✅ Готово! Веб-интерфейс создан

Я создал HTML файл с веб-интерфейсом для подключения к OpenClaw через браузер.

## Шаг 1: Убедитесь что SSH туннель активен

Откройте Терминал и выполните (если еще не запущен):

```bash
ssh -L 18789:127.0.0.1:18789 root@217.25.94.44
```

Введите пароль: `eQR^o3^3WkG4Ki`

**Важно:** Оставьте это окно Терминала открытым!

## Шаг 2: Откройте веб-интерфейс

### Вариант A: Через Finder
1. Откройте Finder
2. Перейдите в папку `/Users/pavelshchepochkin/ii/`
3. Найдите файл `openclaw-web.html`
4. Дважды кликните на него

### Вариант B: Через браузер напрямую
1. Откройте браузер (Chrome, Safari, Firefox)
2. Нажмите `Cmd + O` (или File → Open File)
3. Выберите файл `openclaw-web.html`

### Вариант C: Через терминал
```bash
open /Users/pavelshchepochkin/ii/openclaw-web.html
```

## Шаг 3: Подключитесь

1. В веб-интерфейсе проверьте что URL правильный: `ws://127.0.0.1:18789`
2. Токен уже заполнен (можно оставить как есть)
3. Нажмите кнопку **"Подключиться"**
4. Если подключение успешно, статус изменится на "Подключено" (зеленый)

## Шаг 4: Используйте интерфейс

1. Введите ваше сообщение в текстовое поле внизу
2. Нажмите **"Отправить"** или `Enter`
3. Ответы от OpenClaw будут отображаться в чате

## Возможные проблемы

### "Ошибка подключения"
- Убедитесь что SSH туннель активен
- Проверьте что контейнер OpenClaw запущен: `ssh root@217.25.94.44 'docker ps | grep openclaw'`

### "Соединение закрыто"
- Проверьте логи OpenClaw: `ssh root@217.25.94.44 'docker logs openclaw-gateway'`
- Убедитесь что токен правильный

### Страница не открывается
- Убедитесь что файл существует: `ls -la /Users/pavelshchepochkin/ii/openclaw-web.html`
- Попробуйте открыть через другой браузер

## Что делает веб-интерфейс

- ✅ Подключается к OpenClaw Gateway через WebSocket
- ✅ Отправляет сообщения в формате JSON
- ✅ Получает и отображает ответы
- ✅ Показывает статус подключения
- ✅ Красивый современный интерфейс

## Дополнительная информация

- **WebSocket URL**: `ws://127.0.0.1:18789`
- **Токен**: `9b0dd3b6cff44e7c0fbcb637f6179b9643979eaedefb10da`
- **Файл интерфейса**: `/Users/pavelshchepochkin/ii/openclaw-web.html`

## Быстрая проверка

1. SSH туннель: `ps aux | grep "ssh.*18789"`
2. Контейнер: `ssh root@217.25.94.44 'docker ps | grep openclaw'`
3. Порты: `nc -z 127.0.0.1 18789 && echo "Порт доступен"`

---

**Готово!** Теперь вы можете использовать OpenClaw через браузер! 🎉
